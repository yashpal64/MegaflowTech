import { TransitionTimeout } from "./types";
/**
 * @private
 */
export interface DefinedTimeout {
    appear: number;
    enter: number;
    exit: number;
}
/**
 * @private
 */
export default function getTimeout(timeout: TransitionTimeout, appear: boolean): DefinedTimeout;
