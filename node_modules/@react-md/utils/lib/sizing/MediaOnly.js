"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DesktopOnly = exports.TabletOnly = exports.PhoneOnly = exports.MobileOnly = void 0;
var useAppSize_1 = __importDefault(require("./useAppSize"));
/**
 * A simple component that will render the children only when the app is
 * considered in mobile mode via the `AppSizeContext`. A mobile view will be
 * true for both phones and tablets.
 */
exports.MobileOnly = function (props) {
    var _a = props, children = _a.children, fallback = _a.fallback;
    var _b = useAppSize_1.default(), isPhone = _b.isPhone, isTablet = _b.isTablet;
    if (isPhone || isTablet) {
        return children;
    }
    return fallback;
};
/**
 * A simple component that will render the children only when the app is
 * considered in phone mode via the `AppSizeContext`.
 */
exports.PhoneOnly = function (props) {
    var _a = props, children = _a.children, fallback = _a.fallback;
    var isPhone = useAppSize_1.default().isPhone;
    if (isPhone) {
        return children;
    }
    return fallback;
};
/**
 * A simple component that will render the children only when the app is
 * considered in tablet mode via the `AppSizeContext`.
 */
exports.TabletOnly = function (props) {
    var _a = props, children = _a.children, fallback = _a.fallback;
    var isTablet = useAppSize_1.default().isTablet;
    if (isTablet) {
        return children;
    }
    return fallback;
};
/**
 * A simple component that will render the children only when the app is
 * considered in desktop mode via the `AppSizeContext`.
 */
exports.DesktopOnly = function (props) {
    var _a = props, children = _a.children, fallback = _a.fallback;
    var isDesktop = useAppSize_1.default().isDesktop;
    if (isDesktop) {
        return children;
    }
    return fallback;
};
var defaultProps = {
    fallback: null,
};
exports.MobileOnly.defaultProps = defaultProps;
exports.PhoneOnly.defaultProps = defaultProps;
exports.TabletOnly.defaultProps = defaultProps;
exports.DesktopOnly.defaultProps = defaultProps;
//# sourceMappingURL=MediaOnly.js.map